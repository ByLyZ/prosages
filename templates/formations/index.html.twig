{% extends 'base.html.twig' %}

{% block titreContenu %}
<div class="example-wrapper">
    <h3>Cette page affichera la liste des formations de l'IUT" au sein d'un titre de niveau</h3>
</div>
{% endblock %}
{% block contenu %}
<table class="table">
    <thead>
        <tr>
            <th>
                Formations
            </th>
        </tr>
    </thead>
    {% for formationCourante in formations %}
    <tbody>
        <tr>
            <td>
                <ul>
                    <li>
                        <a href="{{path('stages',{id:formationCourante.id})}}">{{formationCourante.nom}}</a>
                    </li>
                </ul>
            </td>
        </tr>
    </tbody>
    {% endfor %}
</table>
</div>
{% endblock %}