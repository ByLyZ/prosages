{% extends 'base.html.twig' %}

{% block titreContenu %}Formations{% endblock %}

{% block contenu %}
    <h1>Formations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formations_c_r_u_d_index') }}">Retour à la liste des formations</a>

    <a href="{{ path('formations_c_r_u_d_edit', {'id': formation.id}) }}">Editer</a>

    {{ include('formations_crud/_delete_form.html.twig') }}
{% endblock %}
